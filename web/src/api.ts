const api = "http://localhost:8080/api/v1";
const getLocalizationMessage = api + "/resources?lang=";
const registration = api + "/registration";
const login = api + "/login";

export {getLocalizationMessage, registration, login, api};
